import { FC } from "react";
import * as styles from "./styles";

/**
 * Not found page
 * Renders if route can't be founded
 */
const NotFoundPage: FC = () => {
	return <styles.NotFoundContainer>Not Found</styles.NotFoundContainer>;
};

export default NotFoundPage;
